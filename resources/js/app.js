// Intentionally minimal — the app currently has no JS framework or bundled
// interactivity beyond what's written inline in Blade views. This file exists
// so Vite has a JS entry point alongside the CSS build.
